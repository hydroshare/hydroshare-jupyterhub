

this DockerFile is for the HydroShare-JupyterHub base image that is hosted on dockerhub


Build the image  
    
    $ docker build -t hs_jupyer_base .


Once finished, list the images that exist on your machine:

    $ docker images


Tag the image that you want:

    $ docker tag [imageid] castronova/hs-jupyter-base:latest

Upload the image to dockerhub:

    $ docker login
    $ docker push castronova/hs-jupyter-base

